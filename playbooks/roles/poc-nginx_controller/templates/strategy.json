{
  "metadata": {
    "name": "strategy_{{ extra_app.name | lower }}_{{ item.name | lower }}",
    "displayName": "{{ extra_app.name | replace('_', ' ') }}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "content": {
      "securityPolicyRef": "/security/policies/waf_{{ extra_app.name | lower }}_{{ item.name | lower }}"
    }
  }
}